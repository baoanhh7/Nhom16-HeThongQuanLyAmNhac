import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF1DB954); // Spotify Green
  static const Color primaryDark = Color(0xFF191414); // Spotify Black
  static const Color background = Color(0xFF121212); // Dark Background
  static const Color surface = Color(0xFF282828); // Card Background
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color divider = Color(0xFF404040);

  // Gradient colors
  static const List<Color> spotifyGradient = [
    Color(0xFF1DB954),
    Color(0xFF191414),
  ];
}
